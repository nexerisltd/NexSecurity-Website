diff --git a/app/api/video/[id]/download/route.ts b/app/api/video/[id]/download/route.ts
index 7fbb60e..df4959c 100644
--- a/app/api/video/[id]/download/route.ts
+++ b/app/api/video/[id]/download/route.ts
@@ -9,6 +9,12 @@ import { z } from 'zod';
 
 export const dynamic = 'force-dynamic';
 
+// Downloads are currently disabled site-wide (the button was removed from
+// the video page too). Gated behind an env var, rather than deleted or
+// hard-returned, so re-enabling later is a one-line config change and the
+// rest of this route stays live/type-checked instead of going dead code.
+const DOWNLOADS_ENABLED = process.env.ENABLE_VIDEO_DOWNLOADS === 'true';
+
 /**
  * Same authorization gate as /api/video/[id]/play (must be authenticated,
  * authorized, and the video's board must be published) — repeated here
@@ -40,6 +46,10 @@ async function authorizeAndLoadVideo(videoId: string, email: string) {
 /** Lists which resolutions actually exist for this video, so the frontend
  * never offers a resolution that would 404. */
 export async function GET(_req: NextRequest, { params }: { params: { id: string } }) {
+  if (!DOWNLOADS_ENABLED) {
+    return NextResponse.json({ error: 'Downloads are currently disabled.' }, { status: 403 });
+  }
+
   const auth = await requireAuthorized();
   if (!auth.ok) return NextResponse.json({ error: 'Access denied.' }, { status: auth.status });
 
@@ -67,6 +77,10 @@ const downloadRequestSchema = z.object({
 /** Issues one short-lived signed download URL for exactly the requested,
  * already-verified-available resolution. */
 export async function POST(request: NextRequest, { params }: { params: { id: string } }) {
+  if (!DOWNLOADS_ENABLED) {
+    return NextResponse.json({ error: 'Downloads are currently disabled.' }, { status: 403 });
+  }
+
   const auth = await requireAuthorized();
   if (!auth.ok) return NextResponse.json({ error: 'Access denied.' }, { status: auth.status });
 
diff --git a/app/api/video/[id]/hls-download/route.ts b/app/api/video/[id]/hls-download/route.ts
index b694307..1672b13 100644
--- a/app/api/video/[id]/hls-download/route.ts
+++ b/app/api/video/[id]/hls-download/route.ts
@@ -16,6 +16,12 @@ export const maxDuration = 300;
 
 const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? '';
 
+// Downloads are currently disabled site-wide (the button was removed from
+// the video page too). Gated behind an env var, rather than deleted or
+// hard-returned, so re-enabling later is a one-line config change and the
+// rest of this route stays live/type-checked instead of going dead code.
+const DOWNLOADS_ENABLED = process.env.ENABLE_VIDEO_DOWNLOADS === 'true';
+
 /**
  * Some Bunny pull zones have hotlink/User-Agent protection that quietly
  * rejects plain server-to-server fetches (no Referer, generic runtime UA)
@@ -150,6 +156,10 @@ function parseSegments(
 /** GET without ?resolution= lists available resolutions.
  *  GET with ?resolution=240p streams that resolution as a downloadable file. */
 export async function GET(request: NextRequest, { params }: { params: { id: string } }) {
+  if (!DOWNLOADS_ENABLED) {
+    return NextResponse.json({ error: 'Downloads are currently disabled.' }, { status: 403 });
+  }
+
   const auth = await requireAuthorized();
   if (!auth.ok) return NextResponse.json({ error: 'Access denied.' }, { status: auth.status });
 
diff --git a/app/learn/layout.tsx b/app/learn/layout.tsx
index b684c45..464a093 100644
--- a/app/learn/layout.tsx
+++ b/app/learn/layout.tsx
@@ -1,6 +1,7 @@
 import { redirect } from 'next/navigation';
 import { getAuth } from '@/lib/auth';
 import { TopNav } from '@/components/TopNav';
+import { DisableRightClick } from '@/components/DisableRightClick';
 
 export const dynamic = 'force-dynamic';
 
@@ -13,6 +14,7 @@ export default async function LearnLayout({ children }: { children: React.ReactN
 
   return (
     <div className="min-h-screen bg-vault-950">
+      <DisableRightClick />
       <TopNav email={auth.email} isAdmin={auth.user.role === 'ADMIN'} profile={auth.profile} />
       {children}
     </div>
diff --git a/app/learn/video/[id]/page.tsx b/app/learn/video/[id]/page.tsx
index efecbf5..d180f33 100644
--- a/app/learn/video/[id]/page.tsx
+++ b/app/learn/video/[id]/page.tsx
@@ -8,7 +8,6 @@ import { buildBunnyEmbedUrl } from '@/lib/bunny';
 import { buildYoutubeEmbedUrl } from '@/lib/youtube';
 import { logAuditEvent } from '@/lib/audit';
 import { VideoPlayer } from '@/components/VideoPlayer';
-import { VideoDownloadButton } from '@/components/VideoDownloadButton';
 import { PartsList } from '@/components/PartsList';
 
 export const dynamic = 'force-dynamic';
@@ -142,7 +141,8 @@ export default async function VideoPage({ params }: { params: { id: string } })
 
             <div className="mt-6 flex flex-wrap items-start justify-between gap-3">
               <h1 className="font-display text-xl font-semibold text-ink">{video.title}</h1>
-              <VideoDownloadButton videoId={video.id} fallbackUrl={video.download_url} />
+              {/* Download button intentionally removed: video downloads are
+                  disabled for class pages per current policy. */}
             </div>
 
             {resources.length > 0 && (
diff --git a/components/DisableRightClick.tsx b/components/DisableRightClick.tsx
new file mode 100644
index 0000000..6c86ec9
--- /dev/null
+++ b/components/DisableRightClick.tsx
@@ -0,0 +1,22 @@
+'use client';
+
+import { useEffect } from 'react';
+
+/**
+ * Mounted once in app/learn/layout.tsx so it covers every class page
+ * (board listing, video page, ebooks, routines) without repeating this
+ * in each page. Blocks the native context menu only — does not touch
+ * text selection or devtools, since neither can actually be prevented
+ * from the page side and pretending otherwise just adds noise.
+ */
+export function DisableRightClick() {
+  useEffect(() => {
+    function onContextMenu(e: MouseEvent) {
+      e.preventDefault();
+    }
+    document.addEventListener('contextmenu', onContextMenu);
+    return () => document.removeEventListener('contextmenu', onContextMenu);
+  }, []);
+
+  return null;
+}
diff --git a/components/VideoPlayer.tsx b/components/VideoPlayer.tsx
index 61bf426..ec3db6f 100644
--- a/components/VideoPlayer.tsx
+++ b/components/VideoPlayer.tsx
@@ -55,9 +55,43 @@ type YtPlayerInstance = {
   getDuration: () => number;
   getPlayerState: () => number;
   setPlaybackRate: (rate: number) => void;
+  getPlaybackRate: () => number;
+  getAvailablePlaybackRates: () => number[];
+  // Quality controls: YouTube largely auto-manages quality server-side now
+  // and may ignore setPlaybackQuality, but the API is still there and this
+  // keeps the menu functional wherever it is honored.
+  getAvailableQualityLevels: () => string[];
+  getPlaybackQuality: () => string;
+  setPlaybackQuality: (level: string) => void;
   destroy: () => void;
 };
 
+const QUALITY_LABELS: Record<string, string> = {
+  auto: 'Auto',
+  highres: 'Highres',
+  hd2160: '2160p 4K',
+  hd1440: '1440p',
+  hd1080: '1080p',
+  hd720: '720p',
+  large: '480p',
+  medium: '360p',
+  small: '240p',
+  tiny: '144p',
+};
+
+// YouTube's IFrame API has, for years now, mostly stopped honoring manual
+// quality selection and reporting real available levels: on most embeds
+// getAvailableQualityLevels() returns just ['auto'] even when the video
+// itself has every resolution up to 4K — YouTube's own quality-switching
+// logic overrides it. This preset list is shown whenever the API reports
+// 1 or fewer levels, so the menu isn't stuck showing only "Auto" — but
+// selecting one is still just a *request* to YouTube (setPlaybackQuality),
+// which YouTube is free to silently ignore. There is no client-side fix
+// for that; it's a platform restriction, not a bug in this player.
+const QUALITY_PRESETS = ['hd2160', 'hd1440', 'hd1080', 'hd720', 'large', 'medium', 'small', 'tiny', 'auto'];
+
+const SPEED_OPTIONS = [0.25, 0.5, 0.75, 1, 1.25, 1.5, 1.75, 2];
+
 const SEEK_SECONDS = 10;
 const HOLD_THRESHOLD_MS = 320;
 const HEARTBEAT_MS = 4 * 60 * 1000; // well inside the ~10-minute token expiry
@@ -112,6 +146,14 @@ export function VideoPlayer({
   const ytPlayerRef = useRef<YtPlayerInstance | null>(null);
   const ytSeekingRef = useRef(false);
 
+  // --- Settings menu (gear icon): Speed + Quality submenus ---
+  const [settingsOpen, setSettingsOpen] = useState(false);
+  const [settingsPanel, setSettingsPanel] = useState<'main' | 'speed' | 'quality'>('main');
+  const [speed, setSpeedState] = useState(1);
+  const [quality, setQualityState] = useState('auto');
+  const [qualityLevels, setQualityLevels] = useState<string[]>([]);
+  const settingsRef = useRef<HTMLDivElement>(null);
+
   const fetchPlaybackUrl = useCallback(async (): Promise<boolean> => {
     try {
       const res = await fetch(`/api/video/${videoId}/play`, { method: 'POST' });
@@ -252,6 +294,9 @@ export function VideoPlayer({
           setYtDuration(e.target.getDuration());
           setYtMuted(e.target.isMuted());
           setYtBuffering(false);
+          setQualityLevels(e.target.getAvailableQualityLevels?.() ?? []);
+          setQualityState(e.target.getPlaybackQuality?.() ?? 'auto');
+          setSpeedState(e.target.getPlaybackRate?.() ?? 1);
         },
         onStateChange: (e) => {
           setYtPlaying(e.data === window.YT?.PlayerState.PLAYING);
@@ -279,6 +324,12 @@ export function VideoPlayer({
       const yp = ytPlayerRef.current;
       if (!yp) return;
       setYtCurrentTime(yp.getCurrentTime());
+      // Also re-read the real playback quality — YouTube can silently
+      // switch this on its own (bandwidth changes, or just overriding a
+      // manual selection), so this keeps the settings menu honest instead
+      // of showing whatever was last clicked.
+      const liveQuality = yp.getPlaybackQuality?.();
+      if (liveQuality) setQualityState(liveQuality);
     }, YT_TIME_POLL_MS);
     return () => clearInterval(interval);
   }, [isYoutube, ytPlaying]);
@@ -368,6 +419,38 @@ export function VideoPlayer({
     }
   }
 
+  // Close the settings menu on any click outside it (mirrors the pattern
+  // used by VideoDownloadButton's own menu).
+  useEffect(() => {
+    if (!settingsOpen) return;
+    function onClickOutside(e: MouseEvent) {
+      if (settingsRef.current && !settingsRef.current.contains(e.target as Node)) {
+        setSettingsOpen(false);
+        setSettingsPanel('main');
+      }
+    }
+    document.addEventListener('mousedown', onClickOutside);
+    return () => document.removeEventListener('mousedown', onClickOutside);
+  }, [settingsOpen]);
+
+  function changeSpeed(rate: number) {
+    ytPlayerRef.current?.setPlaybackRate(rate);
+    setSpeedState(rate);
+    setSettingsOpen(false);
+    setSettingsPanel('main');
+    showHint(rate === 1 ? 'Normal speed' : `${rate}×`);
+  }
+
+  function changeQuality(level: string) {
+    ytPlayerRef.current?.setPlaybackQuality(level);
+    setQualityState(level);
+    setSettingsOpen(false);
+    setSettingsPanel('main');
+    // Best-effort only: YouTube frequently reverts this to 'auto' on its
+    // own within a second or two, regardless of what was requested here.
+    // The polling above will pick up whatever YouTube actually settles on.
+  }
+
   function toggleMute() {
     const yp = ytPlayerRef.current;
     if (!yp) return;
@@ -540,12 +623,18 @@ export function VideoPlayer({
           )}
 
           {/* Custom control bar — replaces YouTube's native one entirely
-              (controls=0 above). Always visible rather than hover-only,
-              since this needs to work on touch devices with no hover
+              (controls=0 above). Hover-to-reveal on desktop (matches the
+              requested behavior), but always shown while paused or while
+              the settings menu is open — paused-visible is also the
+              practical fallback for touch devices, which have no hover
               state at all. */}
           <div
             onClick={(e) => e.stopPropagation()}
-            className="absolute inset-x-0 bottom-0 flex items-center gap-3 bg-gradient-to-t from-vault-950/90 to-transparent px-3 pb-2 pt-6"
+            className={`absolute inset-x-0 bottom-0 flex items-center gap-3 bg-gradient-to-t from-vault-950/90 to-transparent px-3 pb-2 pt-6 transition-opacity duration-200 ${
+              !ytPlaying || settingsOpen
+                ? 'opacity-100'
+                : 'opacity-0 pointer-events-none group-hover:opacity-100 group-hover:pointer-events-auto'
+            }`}
           >
             <button onClick={togglePlayPause} aria-label={ytPlaying ? 'Pause' : 'Play'} className="text-white">
               {ytPlaying ? (
@@ -560,6 +649,36 @@ export function VideoPlayer({
               )}
             </button>
 
+            <button onClick={() => seek(-SEEK_SECONDS)} aria-label="Back 10 seconds" className="text-white">
+              <svg width="17" height="17" viewBox="0 0 24 24" fill="none">
+                <path
+                  d="M6 12a8 8 0 1 1 2.4 5.7M6 12v5M6 12H1"
+                  stroke="currentColor"
+                  strokeWidth="1.8"
+                  strokeLinecap="round"
+                  strokeLinejoin="round"
+                />
+                <text x="12" y="15" fontSize="7" fill="currentColor" textAnchor="middle" fontFamily="monospace">
+                  10
+                </text>
+              </svg>
+            </button>
+
+            <button onClick={() => seek(SEEK_SECONDS)} aria-label="Forward 10 seconds" className="text-white">
+              <svg width="17" height="17" viewBox="0 0 24 24" fill="none">
+                <path
+                  d="M18 12a8 8 0 1 0-2.4 5.7M18 12v5M18 12h5"
+                  stroke="currentColor"
+                  strokeWidth="1.8"
+                  strokeLinecap="round"
+                  strokeLinejoin="round"
+                />
+                <text x="12" y="15" fontSize="7" fill="currentColor" textAnchor="middle" fontFamily="monospace">
+                  10
+                </text>
+              </svg>
+            </button>
+
             <span className="font-mono text-[10px] text-white/80">{formatTime(ytCurrentTime)}</span>
 
             <input
@@ -598,6 +717,96 @@ export function VideoPlayer({
               )}
             </button>
 
+            <div ref={settingsRef} className="relative">
+              <button
+                onClick={() => {
+                  setSettingsOpen((v) => !v);
+                  setSettingsPanel('main');
+                }}
+                aria-label="Settings"
+                className="text-white"
+              >
+                <svg width="17" height="17" viewBox="0 0 24 24" fill="none">
+                  <path
+                    d="m19.4 13-.1-1-.1-1 1.6-1.3-2-3.4-2 .6-1.7-1-.3-2h-4l-.3 2-1.7 1-2-.6-2 3.4L6.3 11l-.1 1 .1 1-1.6 1.3 2 3.4 2-.6 1.7 1 .3 2h4l.3-2 1.7-1 2 .6 2-3.4L19.4 13Z"
+                    stroke="currentColor"
+                    strokeWidth="1.4"
+                    strokeLinejoin="round"
+                  />
+                  <circle cx="12" cy="12" r="2.6" stroke="currentColor" strokeWidth="1.4" />
+                </svg>
+              </button>
+
+              {settingsOpen && (
+                <div className="glass-panel-solid absolute bottom-7 right-0 z-20 w-40 overflow-hidden rounded-xl py-1 text-xs">
+                  {settingsPanel === 'main' && (
+                    <>
+                      <button
+                        onClick={() => setSettingsPanel('speed')}
+                        className="flex w-full items-center justify-between px-3 py-2 text-left text-ink transition hover:bg-white/70"
+                      >
+                        <span>Speed</span>
+                        <span className="text-ink-faint">
+                          {speed === 1 ? 'Normal' : `${speed}×`} <span aria-hidden>›</span>
+                        </span>
+                      </button>
+                      <button
+                        onClick={() => setSettingsPanel('quality')}
+                        className="flex w-full items-center justify-between px-3 py-2 text-left text-ink transition hover:bg-white/70"
+                      >
+                        <span>Quality</span>
+                        <span className="text-ink-faint">
+                          {QUALITY_LABELS[quality] ?? quality} <span aria-hidden>›</span>
+                        </span>
+                      </button>
+                    </>
+                  )}
+
+                  {settingsPanel === 'speed' && (
+                    <>
+                      <button
+                        onClick={() => setSettingsPanel('main')}
+                        className="flex w-full items-center gap-1 px-3 py-2 text-left font-medium text-ink"
+                      >
+                        <span aria-hidden>‹</span> Speed
+                      </button>
+                      {SPEED_OPTIONS.map((rate) => (
+                        <button
+                          key={rate}
+                          onClick={() => changeSpeed(rate)}
+                          className="flex w-full items-center justify-between px-3 py-2 text-left text-ink transition hover:bg-white/70"
+                        >
+                          {rate === 1 ? 'Normal' : `${rate}×`}
+                          {speed === rate && <span aria-hidden>✓</span>}
+                        </button>
+                      ))}
+                    </>
+                  )}
+
+                  {settingsPanel === 'quality' && (
+                    <>
+                      <button
+                        onClick={() => setSettingsPanel('main')}
+                        className="flex w-full items-center gap-1 px-3 py-2 text-left font-medium text-ink"
+                      >
+                        <span aria-hidden>‹</span> Quality
+                      </button>
+                      {(qualityLevels.length > 1 ? qualityLevels : QUALITY_PRESETS).map((level) => (
+                        <button
+                          key={level}
+                          onClick={() => changeQuality(level)}
+                          className="flex w-full items-center justify-between px-3 py-2 text-left text-ink transition hover:bg-white/70"
+                        >
+                          {QUALITY_LABELS[level] ?? level}
+                          {quality === level && <span aria-hidden>✓</span>}
+                        </button>
+                      ))}
+                    </>
+                  )}
+                </div>
+              )}
+            </div>
+
             <button onClick={toggleFullscreen} aria-label="Fullscreen" className="text-white">
               <svg width="16" height="16" viewBox="0 0 24 24" fill="none">
                 <path
