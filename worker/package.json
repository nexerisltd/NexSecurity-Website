{
  "name": "nexsecurity-stream-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that serves 'm3u8' provider HLS playlists/segments at the edge, off Vercel's bandwidth. See README.md.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.0"
  }
}
