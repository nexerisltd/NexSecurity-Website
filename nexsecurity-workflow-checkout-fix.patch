diff --git a/.github/workflows/build-android.yml b/.github/workflows/build-android.yml
index 57dcc28..244140d 100644
--- a/.github/workflows/build-android.yml
+++ b/.github/workflows/build-android.yml
@@ -112,5 +112,10 @@ jobs:
           git config user.email "github-actions[bot]@users.noreply.github.com"
           git add README.md
           git diff --cached --quiet && echo "No README changes" || git commit -m "chore: update Android download link (build #${{ github.run_number }}) [skip ci]"
+          # The build step touches other tracked files (e.g.
+          # manifest-checksum.txt) without us wanting to commit those —
+          # discard everything except the README commit above so `git
+          # pull --rebase` isn't blocked by "unstaged changes".
+          git checkout -- .
           git pull --rebase origin main || true
           git push origin HEAD:main
